# General Settings
DEBUG=False
SECRET_KEY=change-this-to-a-secure-random-string-in-production
ALLOWED_HOSTS=your-domain.com,www.your-domain.com,localhost,backend
CORS_ALLOWED_ORIGINS=https://your-domain.com
CSRF_TRUSTED_ORIGINS=https://your-domain.com

# Database
POSTGRES_DB=proxyplatform
POSTGRES_USER=postgres
POSTGRES_PASSWORD=secure_postgres_password_here

# Docker Compose (Coolify)
# The port is handled by Coolify's internal proxy, so backend doesn't need to expose ports to host.
# Frontend Nginx exposes 80 internally.
